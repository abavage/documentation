## sts assume role to push to sns topic
# TOKEN cat /var/run/secrets/eks.amazonaws.com/serviceaccount/token
aws sts assume-role-with-web-identity --role-arn "$ROLE_ARN" --role-session-name "AlertmanagerTestSession" --web-identity-token "$TOKEN"

# from the output

export AWS_ACCESS_KEY_ID=""
export AWS_SECRET_ACCESS_KEY=""
export AWS_SESSION_TOKEN=""

curl -X POST https://sns.ap-southeast-2.amazonaws.com/ \
  --user "$AWS_ACCESS_KEY_ID:$AWS_SECRET_ACCESS_KEY" \
  --aws-sigv4 "aws:amz:ap-southeast-2:sns" \
  -H "X-Amz-Security-Token: $AWS_SESSION_TOKEN" \
  -d "Action=Publish" \
  -d "TopicArn=arn:aws:sns:ap-southeast-2:xxxxxxxxxxxx:one-rosa-monitoring-sns-topic" \
  -d "Message=Test notification using manual Web Identity Token exchange" \
  -d "Version=2010-03-31"

# using the above process can use the role locally to test the policy
aws <resource> <action> <options>

# clear data on odf managed disk
wipefs -a /dev/sda 
sgdisk --zap-all /dev/sda 
dd if=/dev/urandom of=/dev/sda bs=1M count=2000

# nmcli bond / tag
nmcli connection add type bond con-name bond0 ifname bond0 bond.options "mode=802.3ad,miimon=100,lacp_rate=fast" ipv4.method disabled ipv6.method disabled
nmcli connection add type ethernet con-name bond0-port1 ifname <device> master bond0
nmcli connection add type ethernet con-name bond0-port2 ifname <device> master bond0

nmcli connection modify bond0-port1 ipv4.method disabled ipv6.method disabled
nmcli connection modify bond0-port2 ipv4.method disabled ipv6.method disabled

nmcli connection add type vlan con-name bond0.1234 ifname bond0.1234 dev bond0 id 1234  ipv4.addresses <ipaddress/mask> ipv4.gateway <gw ip> ipv4.method manual connection.autoconnect yes

nmcli connection up bond0
nmcli connection up bond0-port1
nmcli connection up bond0-port2
nmcli connection up bond0.1234

cat /proc/net/bonding/bond0

# odf check
oc exec -n openshift-storage deploy/rook-ceph-operator -i -t -- /bin/bash -c "export CEPH_CONF=/var/lib/rook/openshift-storage/openshift-storage.config && ceph status"
oc exec -n openshift-storage deploy/rook-ceph-operator -i -t -- /bin/bash -c "export CEPH_CONF=/var/lib/rook/openshift-storage/openshift-storage.config && ceph fs status"
oc exec -n openshift-storage deploy/rook-ceph-operator -i -t -- /bin/bash -c "export CEPH_CONF=/var/lib/rook/openshift-storage/openshift-storage.config && ceph df"
oc exec -n openshift-storage deploy/rook-ceph-operator -i -t -- /bin/bash -c "export CEPH_CONF=/var/lib/rook/openshift-storage/openshift-storage.config && ceph tell mds.ocs-storagecluster-cephfilesystem-a perf dump mds_mem"
oc exec -n openshift-storage deploy/rook-ceph-operator -i -t -- /bin/bash -c "export CEPH_CONF=/var/lib/rook/openshift-storage/openshift-storage.config && ceph osd df tree"
oc get pods -n openshift-storage -o wide  | grep Pend
oc get pods -o wide -n blah | egrep -v 'Running|Completed' | awk '{print $7}'  | sort | uniq -c | sort -nr
oc get pods -n openshift-storage -o wide | egrep 'rook-ceph-mon|rook-ceph-mds-ocs-storagecluster-cephfilesystem|rook-ceph-osd'

# scp ssh
ssh -i .ssh/<some_key> ec2-user@127.0.0.1 -p <port>
scp -i ~/.ssh/id_rsa -P <port> <some_file> ec2-user@127.0.0.1:/tmp/

# patch ingress
oc patch ingresscontroller default -n openshift-ingress-operator --type='merge' -p ' {"spec": {"namespaceSelector": {"matchExpressions": [{ "key": "ingress", "operator": "NotIn", "values": ["apps"]}]}}}'

# patch ingress and add a new key/value
oc patch ingresscontrollers default -n openshift-ingress-operator --type='json' -p'[{"op":"add", "path":"/spec/namespaceSelector/matchExpressions/-", "value": {"key": "ingress", "operator": "NotIn", "values": ["apps1"] }}]'

# find the keepalive apivip
oc logs -c keepalived keepalived-control[1-3] -n openshift-kni-infra | grep -i master

# ACM acm ztp build 
oc get infraenv -A | grep <cluster_name>
oc get agentclusterinstall -A | grep <cluster_name>
# if the cgu is not running remove the ztp-done label from the cluster
oc get cgu -A | egrep "ocp.*11.*"

# aws find instances in region
aws ec2 describe-instance-type-offerings --location-type availability-zone --filters Name=instance-type,Values=g7e.2xlarge --region ap-southeast-1 --output table

# helm
helm repo list
helm repo add helm-gitops  https://abavage.github.io/helm-gitops/
helm search repo
helm search repo <something>
helm pull helm-gitops/namespaces
helm repo update 
helm repo update <repo>



########### machineconfig mc machineconfigpool mcp ###########
# force to use sepecifc / old renedered config

# get a running node in mcp with the 'desired' config
oc get node compute1 -o jsonpath='{.metadata.annotations.machineconfiguration\.openshift\.io/desiredConfig}{"\n"}
oc get node compute1 -o json | jq '.metadata.annotations."machineconfiguration.openshift.io/desiredConfig"'
rendered-worker-xxxxxxxx

oc get node compute1 -o jsonpath='{.metadata.annotations.machineconfiguration\.openshift\.io/currentConfig}{"\n"}
oc get node compute1 -o json | jq '.metadata.annotations."machineconfiguration.openshift.io/currentConfig"'
rendered-worker-xxxxxxxx

# force a reconfigure on the node to match the desired
oc debug node/compute4 -- touch /host/run/machine-config-daemon-force
chroot /host
touch /run/machine-config-daemon-force

# patch the node with the machineconfiguration
oc patch node compute4 --patch '{ "metadata": { "annotations": { "machineconfiguration.openshift.io/desiredConfig": "rendered-worker-xxxxxxxx" } } }'
## this might not be required..?
oc patch node compute4 --patch '{ "metadata": { "annotations": { "machineconfiguration.openshift.io/currentConfig": "rendered-worker-xxxxxxxx" } } }'

# check the status - node should reboot
oc get nodes
oc get mcp
oc get node compute4 -o jsonpath='{.metadata.annotations.machineconfiguration\.openshift\.io/desiredConfig}{"\n"}
oc get node compute4 -o jsonpath='{.metadata.annotations.machineconfiguration\.openshift\.io/currentConfig}{"\n"}

# if it refuses to rollout
oc patch mcp/worker --type merge -p '{"spec":{"paused":true}}'

########### machineconfig end ###########

# lsblk show the disk if it's got a raid config or not as "LOGICAL VOLUME"
lsblk -o +model

## tuned
oc get profiles.tuned.openshift.io -n openshift-cluster-node-tuning-operator

# rando numa
oc get noderesourcetopologies.topology.node.k8s.io
oc get numaresourcesoperators.nodetopology.openshift.io numaresourcesoperator -o yaml

# upgrade info upgrade from -> to version
# https://amd64.ocp.releases.ci.openshift.org/releasestream/4-stable/release/4.20.16?from=4.19.26
oc adm release info 4.19.26 4.20.2 --changelog=changes5  > changelog5.md

# systemcl
systemctl cat kubelet

# sort output wiht column
man column
cat somefile | column -t

#####
more a b
::::::::::::::
a
::::::::::::::
1234
5678
abcd
::::::::::::::
b
::::::::::::::
7890
abcd
2222

Only display unique lines in file a compared to file b
man comm
comm -23 <(sort a) <(sort b)

## another variabtion - finds common lines
cat a.txt a.txt c.txt | sort | uniq -d

#####
# ACS - add log4j with broken image
oc create deploy log4j --image=vulhub/log4j:2.8.1 -- sleep infinity

# oc auth
oc auth can-i --list=true
oc auth can-i <verb> <resource>

# remove terminating project
oc get ns openshift-gitops -o json > gitops.json
vim gitops.json
    "spec": {
        "finalizers": []
    },
oc proxy &
curl -k -H "Content-Type: application/json" -X PUT --data-binary @gitops.json http://127.0.0.1:8001/api/v1/namespaces/openshift-gitops/finalize

# get windows details
net user ab123456 /domain

# tcpdump
tcpdump -nni <interface> arp

# must-gather
oc adm must-gather
oc adm inspect ns/<ns_name>
oc adm must-gather --image=registry.redhat.io/ansible-automation-platform-25/aap-must-gather-rhel8:latest

## AAP postgresql migration
# Create the database in the postgres-13-0 pod
# https://www.postgresql.org/docs/current/app-pgrestore.html
 
CREATE ROLE awx WITH LOGIN PASSWORD '<the_on_prem_passwd>';
SELECT rolname FROM pg_roles ;
CREATE DATABASE awx WITH OWNER awx;

psql -h 127.0.0.1 -p 5432 -U awx -d awx

oc exec -i <postgresql-pod> -- bash -c '
PGPASSWORD="<old_postgres_password>" pg_dump -h <old_postgres_host> -U <old_postgres_user> -d <old_postgres_database> -p <old_postgres_port> -F custom -f /tmp/awx.dump
'
oc exec -i <postgresql-pod> -- bash -c '
PGPASSWORD="<new_postgres_password>" pg_restore -j 4 --clean --if-exists -U <new_postgres_user> -d <new_postgres_database> /tmp/awx.dump
'

## oc selector
oc get pods -A --field-selector spec.nodeName=some_node 

# ACM node delete / rebuild
# Power off the machine - if possible dd the disks (root/odf/lvms etc)

# From the source cluster
oc delete node <node>
oc get machine -A
oc annotate machine <machine-name> -n openshift-machine-api machine.openshift.io/delete-machine=true
# scale down one replica for the deleted node.
# the new node is 'not' added back to the machineset. Its a 'scaled up' node and does not participate in the machineset
oc scale machineset <machineset-name> -n openshift-machine-api --replicas=<new-size>
oc get bmh -A
oc delete bmh <bmh>

# In acm remove the node from the cluster inventory

## Confirm the node is gone from ACM and the cluster
oc get nodes
oc get machines -A
oc get 

## Add the node back via the "Add Hosts" in the cluster inventory - using the "With BMC form"
# The "Baseboard Management Controller Address" needs to be in the form of "redfish-virtualmedia://<bmc ip address>/redfish/v1/Systems/1"

# The node should start - monitor via ACM
# on the cluster approve the CSR's as they appear
oc get csr
oc adm certificate approve <csr>
oc get csr -o name | xargs oc adm certificate approve
oc get nodes
oc get pods -A --field-selector spec.nodeName=some_node

## additional  if the node is stuck 'installing' and can't remove from ACM
oc get agent -A
oc get agent  91152273-66df-464e-3234-bde45f10c3cf -o yaml | yq '.spec.hostname'
oc delete agent 91152273-66df-464e-3234-bde45f10c3cf
